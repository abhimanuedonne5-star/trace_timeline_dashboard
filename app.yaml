command: ["python", "app.py"]

env:
  - name: WAREHOUSE_ID
    value: "your_sql_warehouse_id"   # SQL Warehouses → your warehouse → Overview → ID

  - name: CATALOG
    value: "your_catalog"

  - name: SCHEMA
    value: "your_schema"

  - name: TABLE
    value: "trace_events"
